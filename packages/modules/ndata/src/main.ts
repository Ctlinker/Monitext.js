import { Result } from './lib/result';
import { createResult, createResultableFn } from './lib/result-factory';
import * as DataTypes from './lib/data-types';

export { Result, createResult, createResultableFn, DataTypes };

export const ndata = {
	Result,
	createResult,
	createResultableFn,
};
